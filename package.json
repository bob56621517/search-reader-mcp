{
  "name": "search-reader-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "扩展 jina-reader 镜像:自定义搜索(bocha)+ MCP 服务,单端口整合服务器覆盖默认服务",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.json && node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@koa/bodyparser": "^6.1.0",
    "@types/koa": "^2.15.0",
    "@types/node": "^24.0.0",
    "@types/supertest": "^6.0.0",
    "koa": "^3.1.2",
    "supertest": "^7.0.0",
    "typescript": "^5.5.4"
  }
}
